const TPK_SHEET_CONFIG = Object.freeze({
  providerMode: 'GAS_SHEET_PROVIDER',
  stagingSheets: Object.freeze({
    sasaran: 'staging_sasaran',
    pendampingan: 'staging_pendampingan',
    audit: 'staging_audit_log',
  }),
});
